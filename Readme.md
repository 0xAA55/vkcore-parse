# Vulkan Definition Files Generator by Python3

## Usage
```bash
python3 vkparse.py
```

It will parse `vulkan_core.h` into `vkcore.json`.
- `vkcore.json` is for you to parse it into your language.
